---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: agentbuilds.agent.kagenti.dev
spec:
  group: agent.kagenti.dev
  names:
    kind: AgentBuild
    listKind: AgentBuildList
    plural: agentbuilds
    singular: agentbuild
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentBuild is the Schema for the Agentbuilds API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentBuildSpec defines the desired state of AgentBuild.
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: Annotations to be added to this agent
                type: object
              buildArgs:
                description: BuildArgs are arguments to pass to the build process
                items:
                  description: Parameter defines an argument
                  properties:
                    description:
                      description: Description provides help text for the parameter
                      type: string
                    name:
                      description: Name of the  argument
                      type: string
                    required:
                      description: |-
                        Required indicates if this parameter must be provided by the user
                        Only used in pipeline templates, not in user-provided parameters
                      type: boolean
                    value:
                      description: Value of the argument
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              buildOutput:
                description: BuildOutput specifies where to store build artifacts
                properties:
                  image:
                    description: Image is the name of the image to build
                    type: string
                  imageRegistry:
                    description: ImageRegistry is the container registry where the
                      image will be pushed
                    type: string
                  imageRepoCredentials:
                    description: ImageRepoCredentials is a reference to a secret containing
                      registry credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  imageTag:
                    description: ImageTag is the tag to apply to the built image
                    type: string
                required:
                - image
                - imageRegistry
                - imageTag
                type: object
              cleanupAfterBuild:
                default: true
                description: CleanupAfterBuild indicates whether to automatically
                  cleanup after build
                type: boolean
              labels:
                additionalProperties:
                  type: string
                description: Labels to be added to this agent
                type: object
              mode:
                default: dev
                description: |-
                  Mode specifies which pipeline template to use (dev, dev-local, dev-external, preprod, prod, custom)
                  This will be used to fetch the pipeline template from ConfigMap
                enum:
                - dev
                - dev-local
                - dev-external
                - preprod
                - prod
                - custom
                type: string
              pipeline:
                description: Pipeline specifies the pipeline configuration
                properties:
                  namespace:
                    description: Namespace is the namespace where the pipeline steps
                      ConfigMaps are located
                    type: string
                  parameters:
                    description: Parameters contains additional parameters to pass
                      to the pipeline
                    items:
                      description: Parameter defines an argument
                      properties:
                        description:
                          description: Description provides help text for the parameter
                          type: string
                        name:
                          description: Name of the  argument
                          type: string
                        required:
                          description: |-
                            Required indicates if this parameter must be provided by the user
                            Only used in pipeline templates, not in user-provided parameters
                          type: boolean
                        value:
                          description: Value of the argument
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  steps:
                    description: Steps is an ordered list of pipeline steps to execute
                    items:
                      description: PipelineStepSpec defines a single step in the pipeline
                      properties:
                        configMap:
                          description: ConfigMap references the ConfigMap containing
                            the step definition
                          type: string
                        enabled:
                          description: Enabled indicates whether this step should
                            be included in the pipeline
                          type: boolean
                        name:
                          description: Name is the identifier for the step
                          type: string
                        parameters:
                          description: Parameters contains step-specific parameters
                            that override global parameters
                          items:
                            description: Parameter defines an argument
                            properties:
                              description:
                                description: Description provides help text for the
                                  parameter
                                type: string
                              name:
                                description: Name of the  argument
                                type: string
                              required:
                                description: |-
                                  Required indicates if this parameter must be provided by the user
                                  Only used in pipeline templates, not in user-provided parameters
                                type: boolean
                              value:
                                description: Value of the argument
                                type: string
                            required:
                            - name
                            - value
                            type: object
                          type: array
                        requiredParameters:
                          description: RequiredParameters lists parameter names that
                            users must provide
                          items:
                            type: string
                          type: array
                      required:
                      - configMap
                      - name
                      type: object
                    type: array
                required:
                - namespace
                - steps
                type: object
              source:
                description: Source specifies the source code related configuration
                properties:
                  repoUser:
                    description: RepoUser is the username in the Git repository containing
                      the source
                    type: string
                  sourceCredentials:
                    description: SourceCredentials is a reference to a secret containing
                      Git credentials
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  sourceRepository:
                    description: SourceRepository is the Git repository URL
                    type: string
                  sourceRevision:
                    description: SourceRevision is the Git revision (branch, tag,
                      commit)
                    type: string
                  sourceSubfolder:
                    description: SourceSubfolder is the folder within the repository
                      containing the source
                    type: string
                type: object
            required:
            - source
            type: object
          status:
            description: AgentBuildStatus defines the observed state of AgentBuild.
            properties:
              builtImage:
                type: string
              completionTime:
                description: pipeline completion time
                format: date-time
                type: string
              conditions:
                description: Conditions represent overall status
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastBuildTime:
                description: Last build time
                format: date-time
                type: string
              message:
                description: Build Message
                type: string
              phase:
                description: 'Current build phase: Pending, Building, Succeeded, Failed'
                type: string
              pipelineRunName:
                description: PipelineRun name
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
